services:
  - type: web
    name: media-extractor-backend
    env: docker
    plan: free
    dockerfilePath: ./server/Dockerfile
    envVars:
      - key: PORT
        value: 10000
      - key: API_KEY
        generateValue: true
